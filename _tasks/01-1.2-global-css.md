# Task: Create global.css with full design system
Plan: `/_plans/01-foundation.md` → Step 1.2
Spec: `/_specs/01-foundation.md`

## Context
The design system is the foundation. Every component will reference these variables. Get this right and everything downstream inherits the correct look.

## What to build
A comprehensive global CSS file with variables, font imports, base styles, utility classes, and keyframe animations.

## Technical details

### Google Fonts import
```css
@import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=IBM+Plex+Sans:wght@300;400;500;600&family=IBM+Plex+Mono:wght@400;500&family=Instrument+Serif:ital@0;1&display=swap');
```

### CSS Variables
```css
:root {
  /* Colors */
  --bg: #EEEBE5;
  --bg-card: #F5F3EF;
  --bg-dark: #0C0C0C;
  --text: #111111;
  --text-mid: #555555;
  --text-dim: #999999;
  --border: #D0CCC4;
  --blue: #0038FF;
  --red: #FF2020;
  --green: #00CC55;
  --pink: #FF2D8A;

  /* Typography */
  --font-display: 'Space Grotesk', sans-serif;
  --font-body: 'IBM Plex Sans', sans-serif;
  --font-mono: 'IBM Plex Mono', monospace;
  --font-serif: 'Instrument Serif', serif;
}
```

### Base element styles
- `html`: scroll-behavior smooth
- `body`: background var(--bg), color var(--text), font-family var(--font-body), line-height 1.6, overflow-x hidden
- `*`: box-sizing border-box, margin/padding 0
- `a`: color inherit, text-decoration none
- `ul, ol`: list-style none
- `img`: max-width 100%, display block

### Keyframe animations
- `glitchIn`: clip-path based glitch, 0.35s duration (see CLAUDE.md for exact keyframes)
- `blink`: opacity 1→0→1, step-end, 1s loop
- `fadeInUp`: opacity 0 + translateY(15px) → opacity 1 + translateY(0)

### Utility classes
- `.reveal`: opacity 0, transform translateY(15px), transition opacity 0.45s + transform 0.45s
- `.reveal.visible`: opacity 1, transform translateY(0)
- `.section-label`: font-family var(--font-mono), font-size 0.58rem, color var(--text-dim), uppercase, letter-spacing 0.2em, margin-bottom 2.5rem, flex with accent line `::before` (16px wide, 1px tall, var(--green))

### Responsive base
- `section`: padding 5rem 2.5rem. On mobile: 3.5rem 1.5rem.

## Files to create/modify
- `src/styles/global.css` — create with all of the above

## Acceptance criteria
- All CSS variables available globally
- Fonts load (verify in browser network tab)
- Body has correct background color (#EEEBE5)
- `.reveal` class hides elements by default, `.reveal.visible` shows them
- `.section-label` renders correctly

## Notes
- Don't use `@font-face` — use Google Fonts `@import` for simplicity
- Add `font-display: swap` via the Google Fonts URL parameter (already included in the URL above)

## Status: Done
- Created `src/styles/global.css` with all CSS variables (colors, fonts), Google Fonts import, base resets, keyframes (glitchIn, blink, fadeInUp), `.reveal`/`.reveal.visible` classes, `.section-label` with green accent line, responsive section padding
- Build passes, all tokens present in output
