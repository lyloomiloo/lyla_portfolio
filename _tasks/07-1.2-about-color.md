# Task: Change about section to light theme with green-bordered signal log
Plan: `/_plans/07-design-refinements.md` → Step 1.2
Spec: `/_specs/07-design-refinements.md`

## Context
The dark black about section is too harsh. The whole page should stay light. The signal log panel gets a thin green border to distinguish it, with light background inside and dark text — no more green-on-black terminal aesthetic.

## What to build
Replace the dark terminal panel with a light-themed bordered panel.

## Technical details

### Signal log panel
- Background: var(--bg-card) (#F5F3EF) — same as other cards
- Border: 1px solid var(--green) (#00CC55)
- No dark background anywhere

### Text colors inside signal log (replace all greens with dark theme equivalents)
- Log dates: var(--text-dim) (#999)
- Log labels (SIGNAL_START etc): var(--green) (#00CC55) — these stay green, they're the accent
- Log body text: var(--text) (#111)
- Dim text (.dim): var(--text-dim) (#999)
- Highlight text (.hl): var(--green) (#00CC55)
- Blockquote border-left: 2px solid var(--green)
- Blinking cursor: var(--green) background — stays green

### Header bar
- Background: transparent or var(--bg-card)
- Border-bottom: 1px solid var(--border) (#D0CCC4)
- "signal_log.md" text: var(--text-dim)
- "×" decorative close: var(--text-dim)

### Scrollbar
- Remove custom dark scrollbar styling — use default or style with light colors:
  - Track: transparent
  - Thumb: var(--border) (#D0CCC4)

### Remove from global.css
- Remove `--bg-dark` and `--bg-terminal` variables if they exist — no longer needed

## Files to create/modify
- `src/components/SignalLog.astro` — replace all dark colors with light theme equivalents
- `src/styles/global.css` — remove dark bg variables if present

## Acceptance criteria
- Signal log has light background with green border
- No black/dark backgrounds anywhere on the page
- Log labels and highlights are still green (accent color)
- Body text is dark on light (readable)
- Cursor still blinks in green
- Scrollbar matches light theme
- Whole page feels cohesive — one light theme throughout

## Status: Done
