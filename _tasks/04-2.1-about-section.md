# Task: Build AboutSection.astro and integrate
Plan: `/_plans/04-about.md` → Steps 2.1 + 2.2
Spec: `/_specs/04-about.md`

## Context
Compose the signal log and archive into the two-column about section, add the pullquote, and wire into the index page.

## What to build
A section component that places SignalLog left, ArchivePanel right, with pullquote below. Plus integration into index.astro.

## Technical details

### Two-column grid
- display grid, grid-template-columns 1fr 1fr
- 1px divider: gap 1px, background var(--border)
- SignalLog and ArchivePanel as children (both with their own backgrounds, so the 1px gap shows the border color through)
- On mobile: grid-template-columns 1fr (stack), gap 12px, background none. Children get explicit border.

### Pullquote
Below the grid:
```html
<p class="pullquote reveal">
  "I spent a decade learning to turn noise into signal for brands. Now I design the signal itself."
</p>
```
- font-family var(--font-serif), clamp(1.2rem, 2.5vw, 1.7rem), italic, color var(--text)
- max-width 560px, margin-top 3rem, line-height 1.55
- padding-left 1.2rem, border-left 2px solid var(--green)

### Integration
Add to index.astro:
```astro
<section id="about">
  <div class="section-label reveal">About</div>
  <AboutSection />
</section>
```

## Design tokens
- Pullquote font: var(--font-serif) → Instrument Serif
- Pullquote border: var(--green) → #00CC55
- Grid divider: var(--border) → #D0CCC4

## Files to create/modify
- `src/components/AboutSection.astro` — create
- `src/pages/index.astro` — add about section

## Acceptance criteria
- Two columns on desktop with 1px divider
- Signal log (dark) on left, archive (light) on right
- Pullquote below in serif italic with green left border
- Stacks on mobile
- Scroll to #about works from nav

## Status: Done
