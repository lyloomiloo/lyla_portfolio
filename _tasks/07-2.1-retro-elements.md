# Task: Build RetroElements.astro — animated retro digital decorations
Plan: `/_plans/07-design-refinements.md` → Step 2.1
Spec: `/_specs/07-design-refinements.md`

## Context
The page needs more personality. Animated retro digital elements add the cyberfuturist texture that makes this portfolio memorable.

## What to build
A component (or set of components) with animated retro digital elements, all pure CSS/SVG/HTML — no image files.

## Technical details

### Element 1: Scrolling Marquee Ticker
A horizontal scrolling strip of text, like a stock ticker or news crawl:
```html
<div class="marquee">
  <div class="marquee-inner">
    STRATEGIST → BUILDER · PRODUCT DESIGN · AI INTEGRATION · UX RESEARCH · FRONTEND DEV · CONTENT STRATEGY · BARCELONA · 2025–26 ·
    <!-- repeat for seamless loop -->
  </div>
</div>
```
- Style: var(--font-mono), uppercase, letter-spacing 0.15em, ~0.75rem
- Color: var(--text-dim) 
- Background: transparent or very subtle border-top/bottom
- Animation: translateX(0) → translateX(-50%) linear infinite, ~25s duration
- Full width of page

### Element 2: Pixel Art Character
A small pixel robot or face built from a CSS grid of colored divs:
```html
<div class="pixel-art" style="display:grid; grid-template-columns: repeat(8,6px); gap:1px;">
  <!-- each cell is a 6x6 div with background color or transparent -->
</div>
```
Design a simple 8x8 or 10x10 pixel character using the palette: var(--green), var(--text), transparent, var(--pink).
Should include a subtle animation — blinking eyes (opacity toggle every 3s) or slight bounce.
Size: ~50-60px total.

### Element 3: Blinking REC Indicator
```html
<div class="rec-indicator">
  <span class="rec-dot"></span> REC
</div>
```
- Red dot (var(--red), 8px circle) with blink animation
- "REC" in var(--font-mono), 0.7rem, var(--red)
- Positioned subtly — maybe top-right of a section

### Element 4: Fake Loading Bar
```html
<div class="loading-bar">
  <div class="loading-fill"></div>
</div>
```
- Bar: 120px wide, 6px tall, border 1px solid var(--border)
- Fill: var(--green), animates width 0% → 100% over 3s, then resets
- Small text above: "LOADING..." in var(--font-mono), tiny

### Element 5: ASCII Art Divider
A decorative divider between sections using ASCII/box-drawing characters:
```
─── ◆ ──────────────────────────── ◆ ───
```
Or:
```
[ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ ]
```
- var(--font-mono), var(--text-dim), centered, subtle

### Element 6: Kaomoji / Emoticon
A small animated emoticon that occasionally changes expression:
- Cycles through: (◕‿◕) → (◠‿◠) → (◕ᴗ◕) → (｡◕‿◕｡)
- Changes every 4 seconds
- var(--font-mono), var(--text-dim), small

### Export
Each element should be a named export or a component that accepts a `type` prop:
```astro
<RetroElement type="marquee" />
<RetroElement type="pixel-robot" />
<RetroElement type="rec" />
<RetroElement type="loading-bar" />
<RetroElement type="ascii-divider" />
<RetroElement type="kaomoji" />
```

### Performance
- All CSS animations, no JS except the kaomoji text swap (tiny inline script)
- On mobile: hide pixel-robot, simplify marquee
- Use `will-change: transform` on marquee for GPU acceleration

## Design tokens
- Green: var(--green) → #00CC55
- Red: var(--red) → #FF2020
- Pink: var(--pink) → #FF2D8A
- Dim text: var(--text-dim) → #999
- Mono font: var(--font-mono)

## Files to create/modify
- `src/components/RetroElements.astro` — create with all 6 element types

## Acceptance criteria
- All 6 element types render correctly
- Marquee scrolls smoothly and loops seamlessly
- Pixel art character is visible and has subtle animation
- REC dot blinks
- Loading bar fills and resets
- ASCII divider renders cleanly
- Kaomoji changes expression
- No jank or performance issues

## Status: Done
