# Task: Build WorkTile.astro
Plan: `/_plans/04-about.md` → Step 1.2
Spec: `/_specs/04-about.md`

## Context
Small tile for past advertising work. Used in the archive panel's 2×3 grid.

## What to build
A tiny card component for a single past project.

## Technical details

### Props
- name: string (project name)
- client: string (e.g., "Scoot · BLKJ")
- note: string (award or descriptor)

### Structure
```html
<div class="work-tile">
  <div class="work-tile-name">{name}</div>
  <div class="work-tile-client">{client}</div>
  <div class="work-tile-note">{note}</div>
</div>
```

### Styles
- Tile: background var(--bg-card), padding 9px 10px (no explicit border — handled by grid parent)
- Name: var(--font-display), 0.64rem, weight 700, var(--text), line-height 1.3
- Client: var(--font-mono), 0.48rem, var(--text-dim), uppercase, letter-spacing 0.03em
- Note: var(--font-mono), 0.46rem, var(--red), uppercase, letter-spacing 0.03em, margin-top 2px

## Design tokens
- Name font: var(--font-display) → Space Grotesk
- Note color: var(--red) → #FF2020

## Files to create/modify
- `src/components/WorkTile.astro` — create

## Acceptance criteria
- Tile renders all three text lines
- Note text is red
- Compact sizing

## Status: Done
