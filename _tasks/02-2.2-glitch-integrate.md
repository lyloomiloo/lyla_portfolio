# Task: Integrate GlitchText into Hero
Plan: `/_plans/02-hero.md` → Step 2.2
Spec: `/_specs/02-hero.md`

## Context
Wire up the GlitchText component to wrap the hero title.

## What to build
Replace the static `<h1>` in Hero.astro with the GlitchText island component.

## Technical details
- Import GlitchText in Hero.astro
- Replace the static h1 with: `<GlitchText client:load text="LYLA\nHUANG." tag="h1" class="hero-title" />`
- Ensure the hero-title styles still apply (font-size, weight, line-height, letter-spacing)
- Test on multiple viewport widths — the clamp() font size should still work
- Verify no layout shift: the title container should not jump when characters swap

## Files to create/modify
- `src/components/Hero.astro` — replace static h1 with GlitchText

## Acceptance criteria
- Title renders with glitch animation active
- Looks identical to the static version when not glitching
- Responsive sizing preserved
- No console errors

## Status: Done
- Replaced static h1 with GlitchText component in Hero.astro
