# Task: Initialize Astro project and install dependencies
Plan: `/_plans/01-foundation.md` → Step 1.1
Spec: `/_specs/01-foundation.md`

## Context
First task. Nothing exists yet. Set up the Astro project so everything else can build on it.

## What to build
A working Astro project with TypeScript strict mode, sitemap integration, and the correct project structure.

## Technical details
- Run `npm create astro@latest` with TypeScript strict, no sample files
- Install `@astrojs/sitemap`
- Configure `astro.config.mjs` with site URL and sitemap integration
- Ensure `tsconfig.json` extends `astro/tsconfigs/strict` with `@/*` path alias
- Create empty directory structure: `src/layouts/`, `src/components/`, `src/styles/`, `src/pages/projects/`, `public/images/projects/{reroute,snapp,planmytrip}/`

## Files to create/modify
- `package.json` — verify deps
- `astro.config.mjs` — site + sitemap
- `tsconfig.json` — strict + path alias
- Directory structure created

## Acceptance criteria
- `npm run dev` starts without errors
- `localhost:4321` shows a blank page (or Astro default)

## Notes
- Don't add sample/demo content — we're building from scratch
- The project content files already exist in `src/content/` — don't overwrite them

## Status: Done
- Dependencies installed (astro 5.18.1, @astrojs/sitemap)
- tsconfig.json has strict mode + `@/*` path alias
- astro.config.mjs has site URL + sitemap
- Directory structure created: `src/{layouts,components,styles,pages}`, `public/images/projects/{reroute,snapp,planmytrip}`
- Minimal `src/pages/index.astro` placeholder created
- Fixed `src/content/config.ts` to use Astro 5 glob loader (was using deprecated `type: 'content'`)
- `npm run build` succeeds cleanly
