---
interface Props {
  href: string;
  text: string;
  variant?: 'default' | 'primary';
  target?: string;
}

const { href, text, variant = 'default', target } = Astro.props;
---

<a href={href} class={`retro-btn ${variant === 'primary' ? 'retro-btn-primary' : ''}`} target={target} rel={target === '_blank' ? 'noopener noreferrer' : undefined}>{text}</a>

<style>
  .retro-btn {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    padding: 8px 16px;
    cursor: pointer;
    text-decoration: none;
    background: transparent;
    color: var(--text);
    border: 2px solid var(--text);
    transition: background 0.15s, color 0.15s, transform 0.1s;
  }

  .retro-btn:hover {
    background: var(--text);
    color: var(--bg);
    transform: translateY(-1px);
  }

  .retro-btn-primary {
    background: var(--orange);
    color: #fff;
    border-color: var(--orange);
  }

  .retro-btn-primary:hover {
    background: var(--text);
    color: var(--bg);
    border-color: var(--text);
  }

  .retro-btn:active {
    transform: translateY(1px);
  }
</style>
